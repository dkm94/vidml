import { DesktopNavbarProps } from '@/types';
import React, { FC } from 'react';

const DesktopNavbar: FC<DesktopNavbarProps> = ({ handleShowLinks, showLinks, path }) => {
	return (
		<div className='w-12'>DesktopNavbar</div>
	);
};

export default DesktopNavbar;